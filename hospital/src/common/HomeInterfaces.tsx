export interface CardProps {
    title: string
    image: string
    update: string
  }

export interface LessonModel {
    title: string
    imageUrl: string
    videoUrl: string
    info: string
    updatedAt: string
  }